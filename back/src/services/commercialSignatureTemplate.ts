/**
 * Contenu par défaut de la signature commerciale ajoutée en fin de mail
 * « AB à signer ». Stockée individuellement par commercial (une par user).
 * Contient la variable {{signature}} remplacée par l'image de signature Drive.
 */
export const DEFAULT_COMMERCIAL_SIGNATURE = `<p>Cordialement,</p><p>Commercial Disciplina<br>{{signature}}</p>`;

export enum ExternalLinkStatus {
    PENDING = 'PENDING',
    AUTHENTICATED = 'AUTHENTICATED',
    COMPLETED = 'COMPLETED',
    LOCKED = 'LOCKED',
    EXPIRED = 'EXPIRED',
}

export enum GuestType {
    COMPANY = 'COMPANY',
    CANDIDATE = 'CANDIDATE',
}
